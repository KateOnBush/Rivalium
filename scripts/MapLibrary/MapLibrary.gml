#macro MAPS global.maps

function Map(_backgrounds = []) constructor {

	backgrounds = _backgrounds;

}



global.maps = [

	new Map([
	
		BackgroundLayer0,
		BackgroundLayer1,
		BackgroundLayer2,
		BackgroundLayer3,
		BackgroundLayer4
	
	])
	
]